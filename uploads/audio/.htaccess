# Deny direct access to audio files
# Audio must be accessed through stream.php

Order deny,allow
Deny from all

# Disable PHP execution
<FilesMatch "\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Additional security
<IfModule mod_php.c>
    php_flag engine off
</IfModule>

Options -Indexes
